{
  "name": "@slotflow/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@slotflow/policy-engine": "workspace:*",
    "@slotflow/route-adapters": "workspace:*",
    "@slotflow/shared": "workspace:*",
    "@slotflow/tx-monitor": "workspace:*",
    "fastify": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
